@model DarkLibCW.Models.Status

@{
    ViewData["Title"] = "Детализация";
}

<div>
    <h4>Статус: </h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Type)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Type)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-edit" asp-action="Edit" asp-route-id="@Model?.Id">Изменить</a>
    <a class="btn btn-back" asp-action="Index">Вернуться к списку</a>
</div>
