@model IEnumerable<DarkLibCW.Models.Booking>

@{
    ViewData["Title"] = "Бронирование";
}

<h1>Таблица Бронирования книг</h1>

<p>
    <a class="btn btn-1" asp-action="Create">Добавление новой записи</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Book)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subscriber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Book.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subscriber.Id)
            </td>
            <td>
                    <a class="btn btn-edit" asp-action="Edit" asp-route-id="@item.Id">Изменить</a>
                    <a class="btn btn-edit" asp-action="Details" asp-route-id="@item.Id">Детализация</a>
                    <a class="btn btn-delete" asp-action="Delete" asp-route-id="@item.Id">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
