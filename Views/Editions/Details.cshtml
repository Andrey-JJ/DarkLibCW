@model DarkLibCW.Models.Edition

@{
    ViewData["Title"] = "Детализация";
}

<h4>Издательство:</h4>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-edit" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-edit" type="submit">Изменить</a>
    <a class="btn btn-back" asp-action="Index" class="btn btn-back" type="submit">Вернуться к списку</a>
</div>
