@model DarkLibCW.Models.ViewModels.BookData

@{
    ViewData["Title"] = "Детализация";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    <h4>Книга: </h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Book.CatalogCard)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Book.CatalogCard.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Book.Status)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Book.Status.Type)
        </dd>
    </dl>
</div>
<hr />
<h4>Информация о выдаче книги: </h4>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Issues.First().Subscriber.FullName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Issues.First().Librarian.FullName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Issues.First().IssueDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Issues.First().ReturnDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Issues)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Subscriber.FullName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Librarian.FullName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IssueDate.Date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ReturnDate.Date)
                </td>
            </tr>
        }
    </tbody>
</table>
<hr />
@*<h4>Информация о бронировании книги: </h4>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Bookings.First().Subscriber.FullName)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Bookings)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Subscriber.FullName)
                </td>
            </tr>
        }
    </tbody>
</table>
<hr />*@
<div>
    <a asp-action="Edit" asp-route-id="@Model.Book.Id" class="btn btn-edit" type="submit">Изменить</a>
    <a asp-action="Index" class="btn btn-back" type="submit">Вернуться к списку</a>
    <a asp-action="GetReport" asp-route-id="@Model.Book.Id" class="btn btn-1" type="submit">Получить отчет о книге</a>
</div>